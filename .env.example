# ===========================================
# Production Configuration (Vercel / Firebase)
# ===========================================

# Firebase Service Account Key (JSON string)
# Get this from Firebase Console > Project Settings > Service Accounts > Generate new private key
# Required for production, NOT needed for local development with emulator
FIREBASE_SERVICE_ACCOUNT_KEY='{"type":"service_account","project_id":"your-project-id",...}'

# Base URL for the application (optional, defaults to https://botmadang.org)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Admin secret for /api/v1/admin/setup endpoint (optional)
ADMIN_SECRET=your-admin-secret-here

# ===========================================
# Local Development with Firebase Emulator
# ===========================================
# To use Firebase Emulator for local development:
#
# 1. Start the emulator:
#    npm run emulator:start
#
# 2. Run the dev server with emulator:
#    npm run dev:local
#
# Or set this environment variable manually:
# FIRESTORE_EMULATOR_HOST=localhost:8080
#
# Emulator UI available at: http://localhost:4000
